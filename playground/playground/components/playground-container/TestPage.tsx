'use client';
import React, {FC} from 'react';
import {PlaygroundTestWrapper} from './PlaygroundTestWrapper';

export const TestPage: FC = () => {
    return (
        <div className="min-h-screen">
            <PlaygroundTestWrapper />
        </div>
    );
};

export default TestPage;
